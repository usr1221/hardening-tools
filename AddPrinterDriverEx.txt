$Signature = @"
using System;
using System.Runtime.InteropServices;

public class WinSpool {
    [DllImport("winspool.drv", CharSet = CharSet.Unicode, SetLastError = true)]
    public static extern bool AddPrinterDriverEx(
        string pName,
        uint Level,
        IntPtr pDriverInfo,
        uint dwFileCopyFlags
    );
}
"@

Add-Type -TypeDefinition $Signature

# Call the API directly
$result = [WinSpool]::AddPrinterDriverEx($null, 2, [IntPtr]::Zero, 0)

# Capture the exact Win32 return code
$lastError = [System.Runtime.InteropServices.Marshal]::GetLastWin32Error()
$exception = New-Object System.ComponentModel.Win32Exception($lastError)

[PSCustomObject]@{
    ApiResult = $result
    ErrorCode = $lastError
    ErrorName = $exception.NativeErrorCode
    Message   = $exception.Message
}
